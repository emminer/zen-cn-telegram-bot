{
  "name": "zen-cn-telegram-bot",
  "version": "1.0.0",
  "description": "Zen中文机器人",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "switch_to_dev": "export $(cat .env | xargs) && wget --spider https://api.telegram.org/bot$TOKEN/setWebhook?url= --delete-after && node index.js"
  },
  "author": "Bob Yuan",
  "license": "MIT",
  "repository": "https://github.com/emminer/zen-cn-telegram-bot",
  "dependencies": {
    "dotenv": "^4.0.0",
    "telegraf": "^3.24.0"
  }
}
